import { ShowCardDirective } from './show-card.directive';

describe('ShowCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowCardDirective();
    expect(directive).toBeTruthy();
  });
});
